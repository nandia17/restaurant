import React from "react";
import "./style.css"

function Reser(props) {
    return (  
        <div className="Res-text">
            <h3>{props.text}</h3>
        </div>
    );
}

export default Reser;

